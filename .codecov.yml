  
coverage:
    range: 80..100
    round: down
    precision: 2

    status:
        project:
            default:
                enabled: yes
                target: auto

                threshhold: 2%
                if_not_found: success
                if_ci_failed: error
            patch:
                default:
                    enabled: no


ignore:
    - "test"

